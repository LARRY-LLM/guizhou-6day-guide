# 贵州 6 日旅行攻略项目进度

## 2026-07-17 完整攻略换版决策

- 用户提供 `D:/workbuddy/project/2026-07-16-16-34-33/贵州6天攻略_完整整合版.md`，要求把其中所有行程、酒店、餐饮、价格、交通、预约和避坑内容完整迁入现有网站。
- 用户确认继续在原 React + Vite 网站上修改，不重新搭建；保留“黔蓝手作旅行簿”风格、响应式、打印和原发布方式。
- 新路线替换旧路线：D1 贵阳，D2 黄果树/安顺，D3 龙宫/屯堡后回贵阳，D4 西江，D5 朗德上寨并续住西江，D6 返回贵阳半日后返程；旧小七孔内容退出。
- 用户要求增加六日行程地图，并从三版视觉模板中选择第 2 版：`C:/Users/LarryLiang/.codex/generated_images/019f6df4-1123-7b50-bac5-2be44378f5e6/exec-852d6737-bbf6-456d-a6dd-b1cdef73b2da.png`。
- 已形成设计规格 `docs/superpowers/specs/2026-07-17-complete-guide-route-map-design.md` 和实施计划 `docs/superpowers/plans/2026-07-17-complete-guide-route-map.md`；尚未修改生产代码。

## 项目目标

基于 React + Vite 制作“黔蓝手作旅行簿”风格的贵州 6 日静态攻略，并在最终阶段生成可离线打开、适合打印为 PDF 的 `dist/` 交付物。

## 总体进度

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 内容、数据与项目基线 | 已通过 |
| 2 | 首屏与路线视觉 | 已通过 |
| 3 | 完整攻略与移动端 | 已通过 |
| 4 | 打印、构建与最终 QA | 进行中：网站已恢复，待打印 PDF 人工核查与用户验收 |

## 阶段 1：内容、数据与项目基线

### 本阶段目标

- 复用现有 React/Vite 工程、页面组件、测试框架和本地图片。
- 将 D5 调整为晚上返回贵阳。
- 将 D6 调整为上午贵阳城区漫游、下午高铁返程。
- 核对核心官方信息来源，保留易变信息的出发前复核提示。
- 建立可持续维护的项目进度记录。

### 当前状态

阶段内容已完成并经用户确认，已经进入阶段 2。

### 已完成内容

- 建立设计规格、阶段 1 实施计划和本项目进度档案。
- 将长期有效的视觉、路线、D5/D6 和阶段验收决策写入 `AGENTS.md`。
- D5 已改为清晨至下午继续游览西江，傍晚经凯里南返回贵阳，晚上入住贵阳。
- D6 已改为上午文昌阁、电台街轻量城市漫游，午后取行李并乘高铁返程。
- 新增两项 D5/D6 用户可见内容测试；先观察到两项测试失败，再更新数据使其通过。
- 将黄果树来源更新为 2026 年 5 月 5 日发布的水帘洞实名分时预约专项说明。
- 保留小七孔官网与西江官网链接，并记录网络环境的复核限制。

### 已确认可复用内容

- React + Vite 工程、依赖和 Vitest 测试环境。
- `App.jsx` 中的页面语义结构、固定锚点、打印入口和官方来源区域。
- `itinerary.js` 的 `DayPlan` 数据结构及 D1–D4 内容。
- 黄果树、小七孔、西江、贵阳及纸张/靛蓝装饰共 7 张本地图片。

### 已发现问题

1. `styles.css` 只有基础重置和旧移动端容器样式，暂不具备参考稿视觉。
2. D5/D6 曾是调整前版本；已在阶段 1 通过测试先行完成修正。
3. 项目目录没有 Git 仓库，无法建立工作树或生成阶段提交。
4. 尚无 `dist/`；按计划仅在最终阶段生成正式交付物。
5. 当前 Windows 网络环境对小七孔和西江官网的直接 HEAD 请求出现 SSL/TLS 信任错误，但搜索索引仍可读取官网页面；这不能直接判定官网失效。
6. 首次文档占位符扫描把计划中用于说明检查命令的示例词本身识别为未完成项，产生假阳性。

### 解决办法

1. CSS 留到阶段 2/3 依据视觉稿重建，本阶段不提前改动。
2. 使用测试先行更新 D5/D6，避免后续视觉开发覆盖行程要求。
3. 直接在用户指定的现有项目中修改，用本进度文件记录每次阶段变更。
4. 阶段 1 使用 Vitest 验证内容基线，阶段 4 再执行正式构建和打印 QA。
5. 小七孔与西江链接暂时保留，在阶段 4 用真实浏览器再次检查；若仍无法打开，再替换为同一景区的可访问官方公告页。
6. 占位符扫描使用分段字符模式匹配目标词，避免检查规则扫描并命中自身示例。

### 可复用思路

- 将行程文案集中在 `DayPlan` 数组中，视觉组件只负责渲染，便于以后更换天数或路线。
- 用用户可见文本测试关键节奏，而不是测试组件内部实现。
- 将易变的车次、票价和开放状态统一写成“出发前复核”，避免静态攻略快速过期。
- 外部链接检查需要区分“页面失效”和“本机 TLS 信任失败”，不能把后者误报成坏链接。
- 自动化文档检查要避免规则文本自匹配，尤其是扫描占位符、密钥模式或禁用词时。

### 官方来源状态（2026-07-16）

| 来源 | 状态 | 处理 |
| --- | --- | --- |
| [黄果树水帘洞预约说明](https://www.gzastv.cn/ac/b/content_193320.shtml) | HTTP 200；2026-05-05 发布 | 已替换为更精准的专项说明 |
| [荔波小七孔景区官网](https://www.liboxiaoqikong.com/) | 搜索索引可读取；本机直连遇到 TLS 信任错误 | 保留，阶段 4 浏览器复核 |
| [西江千户苗寨推荐动线](https://www.xjqhmz.com/news/detail?id=828296979686727682&type=news-notice) | 官网域名及预约公告可检索；本机直连遇到 TLS 信任错误 | 保留，阶段 4 浏览器复核 |

### 阶段验收结果

- Vitest：6 项测试通过。
- 行程结构：D1–D6 保持完整，D2–D5 为四个主要游玩日。
- 本阶段未修改 `styles.css`，未生成 `dist/`，符合阶段边界。

## 后续缺口的优先级与依赖关系

| 缺口 | 当前结论 | 优先级 | 安排 |
| --- | --- | --- | --- |
| `PROJECT_PROGRESS.md` | 已建立并持续维护 | 已解决 | 每阶段结束时更新 |
| 桌面端正式视觉 | 是下一阶段的核心工作，但不是阶段 2 开始前的额外阻塞 | P0（阶段 2） | 先完成首屏、导航和路线条的 1440px 桌面视觉 |
| 390px 手机端 | 依赖桌面设计系统与完整内容布局，不宜抢在阶段 2 前实现 | P1（阶段 3） | 完整攻略完成后转为单列时间轴并消除横向滚动 |
| 打印样式 | 依赖最终页面结构和分页内容，不宜现在实现 | P1（阶段 4） | 隐藏交互控件并完成 PDF 打印预览检查 |
| `dist/` 生产交付物 | 不是开发前置条件；现在生成只会得到无正式视觉的半成品 | P1（阶段 4） | 最终测试和视觉 QA 通过后执行生产构建 |
| 视觉稿还原与视觉 QA | 必须建立在视觉 CSS 和响应式布局完成之后 | 阶段 2–4 持续执行 | 每阶段做局部对照，阶段 4 完成 P0–P2 总验收 |

### 对下一阶段的影响判断

- 阶段 2 没有待补的技术前置阻塞，可以直接开始首屏与路线视觉。
- 当前 `styles.css` 只有 292 字节，是阶段 2 要解决的主体，不应作为独立的“阶段前修复”重复开展。
- 不提前生成 `dist/`、打印样式或手机端布局，避免后续结构调整导致重复返工。

### 用户检查意见

用户确认可以在现有基础上进入阶段 2。

## 阶段 2：首屏与路线视觉

### 本阶段目标

- 以选定视觉稿为唯一基准，完成桌面端顶部导航、首屏摄影拼贴和 D1–D6 路线速览。
- 在 1024px 参考宽度与 1440px 目标桌面宽度完成浏览器视觉 QA。
- 保持完整日程、390px 手机端、打印样式和 `dist/` 在后续阶段处理。

### 当前状态

阶段内容已完成并经用户确认，已经进入阶段 3。

### 已完成内容

- 重建页面视觉系统：米白纸张纹理、靛蓝标题、朱红操作按钮、手写感中文字体和旅行簿纸面阴影。
- 完成桌面端顶部导航与左文右图首屏布局，复用黄果树、小七孔、西江和靛蓝织物本地资源。
- 完成 D1–D6 六节点路线速览，并用 Phosphor 图标补足路线的旅行手记感。
- 新增 CSS 视觉契约测试和路线图标数量测试；Vitest 共 7 项通过。
- 完成 1024px、1440px 浏览器检查，两个宽度均无横向滚动。
- 验证“展开这趟旅程”可进入 `#itinerary`，浏览器控制台无 warning/error。
- 生成整体和路线局部并排对照图，并将 QA 结论写入 `design-qa.md`。

### 遇到问题与解决办法

1. 初版首屏纵向位置比参考稿低，路线卡在参考视口中露出不足。改为桌面首屏顶部对齐并收紧文字区上边距。
2. 靛蓝织物的负右偏移造成约 24px 横向溢出。将其限制在拼贴容器边界内，并用两个桌面宽度复核 `scrollWidth`。
3. 纯文字路线节点与参考稿的手绘地标相比层次偏弱。复用项目已有图标库，为六天分别配置语义化路线图标。
4. 本地素材没有参考稿中的红色织物和植物贴纸。将其记录为不影响阶段目标的 P3 差异，避免用 CSS 绘图或占位图伪造素材。

### 可复用思路

- 先在与参考稿相同的 1024px 宽度校准构图，再到 1440px 验证目标桌面布局，可区分“还原偏差”和“响应式偏差”。
- 视觉 QA 应同时保留整页并排图和重点区域局部图，避免只凭单张截图判断。
- 用 `clientWidth === scrollWidth` 作为桌面无横向溢出的自动检查信号，比肉眼观察滚动条更稳定。
- 图标映射与行程数据分离，既保持 `DayPlan` 数据纯净，也便于以后替换某一天的视觉符号。
- 素材不足时明确降级边界并记录 P3 差异，不用代码绘图假装真实装饰资产。

### 阶段验收结果

- Vitest：7 项测试通过。
- 视觉证据：`design-qa/stage2-comparison-final.png`、`design-qa/stage2-route-comparison.png`。
- 页面宽度：1024px 与 1440px 均无横向溢出。
- 交互与控制台：主按钮锚点成功，控制台无 warning/error。
- `design-qa.md`：`final result: passed`。
- 本阶段未生成 `dist/`，未进入完整日程、手机端和打印样式，符合阶段边界。

## 阶段 3：完整攻略与移动端

### 本阶段目标

- 完成 D1–D6 详细日程、三大景点、交通住宿、预算美食、行前清单与官方来源的正式视觉。
- 将 390px 手机端转为单列时间轴，并保证无横向滚动。
- 延续阶段 2 的“黔蓝手作旅行簿”设计系统，不提前制作打印样式和生产交付物。

### 当前状态

阶段内容已完成并经用户确认；已进入阶段 4。

### 已完成内容

- 将六天日程做成旅行纸页：照片、分段时间轴、交通住宿信息与手记边注形成稳定三栏结构。
- 完成三大景点三联相纸卡，以及交通住宿、预算美食、雨季装备、预约顺序和雨天替代模块。
- 为完整内容增加稳定测试标记，自动核对 6 个日程页、22 个时间段、4 个实用卡和 3 个清单卡。
- 在 640px 以下将首屏、路线、日程、景点和实用信息收束为单列；路线条转为六节点纵向速览。
- 在 1440px 与 390 × 844 下完成浏览器视觉 QA，并保存桌面关键区块、手机状态总表及参考稿对照图。
- 验证手机端“行前准备”导航可用，15 张图片全部加载，控制台无 warning/error。

### 遇到问题与解决办法

1. 长页面使用平滑滚动时，初次截图在动画结束前执行，导致证据落在目标区块之前。延长锚点后的等待时间并重新捕获对应区块。
2. 首次加载只触发了 12/15 张懒加载图片。逐段访问景点、交通和清单锚点后再次核验，最终 15/15 加载完成。
3. 手机顶部导航需要在有限宽度内保持可操作。使用单行可横向滚动的导航容器，实际 390px 状态下五个入口均可见，同时不扩大页面宽度。
4. 桌面三栏日程在平板宽度下会过密。999px 以下先转为照片+时间轴两栏，640px 以下再转为单列，边注跟随内容自然下移。

### 可复用思路

- 长页面视觉 QA 要按锚点分别捕获，且平滑滚动必须等待稳定后再截图，否则证据与目标状态不一致。
- 懒加载图片不能只检查首屏；应访问所有图片密集区块，再统一核对 `complete` 与 `naturalWidth`。
- 响应式适配可以采用桌面三栏、平板两栏、手机单列的渐进收束，避免只在一个断点突然重排全部内容。
- 用内容数量测试保护攻略完整性，用 CSS 契约测试保护断点与关键视觉选择器，再用真实浏览器验证布局结果。
- 手机无横向溢出同时检查文档宽度和越界元素列表，可发现被 `overflow-x: hidden` 掩盖的内容问题。

### 阶段验收结果

- Vitest：9 项测试通过。
- 1440px：无横向溢出。
- 390px：文档无横向溢出，越界元素扫描为空。
- 图片：15/15 加载完成。
- 导航与控制台：`#checklist` 锚点成功，控制台无 warning/error。
- 视觉 QA：`design-qa.md` 的阶段 3 `final result: passed`。
- 本阶段未生成 `dist/`，未完成打印样式，符合阶段边界。

## 阶段 4：打印、构建与最终 QA

### 当前状态

打印样式、生产构建和公开托管配置已完成。版本 2 的公网请求全部返回 HTTP 404；沿“域名 → Worker → 静态资源绑定 → 浏览器文件”的请求链路复核后，确认根因是 Vite 将静态文件输出到 `dist/` 根目录，而 Sites 只从 `dist/client/` 建立 `ASSETS` 绑定。最小修复已作为版本 3 发布，桌面端公开网站已经恢复。随后用户在手机浏览器访问同一地址时被 Cloudflare 安全页拦截；代码、移动端 User-Agent 请求和 Sites 公开状态均正常，问题定位为托管边缘对手机网络出口 IP 或请求特征的拦截。GitHub Pages 自动构建现已发布成功，主要手机分享地址为 <https://larry-llm.github.io/guizhou-6day-guide/>，Sites 版本 3 保留为备用入口；仅待用户在原手机网络验收和打印另存 PDF 人工核查。

### 已完成内容

- 完成“打印 / 保存攻略”入口与 A4 打印 CSS：打印时隐藏交互控件，并为长页内容提供适合另存为 PDF 的版式。
- 补齐页面标题、描述和相对资源路径；生成 `dist/` 生产产物，离线打开时资源路径可正确解析。
- 为 Sites 增加 Cloudflare Worker 静态资源入口、`.openai/hosting.json` 与构建复制逻辑。
- 新增托管兼容性测试，并将锁文件中的 `registry.npmmirror.com` 下载源替换为官方 `registry.npmjs.org`；解决 Sites 对不受支持依赖源的构建拒绝。
- 初始化 Git 仓库并推送两次源码版本；Sites 项目已设置为公开访问。
- 已创建并部署 Sites 版本 2，平台状态为 `succeeded`，生成的候选公开地址为 <https://guizhou-six-day-guide.rvsinw.chatgpt.site>。
- 新增 `dist/client/` 产物契约回归测试；先确认旧配置下测试失败，再将 Vite 输出目录改为 `dist/client/`，保持 `dist/server/index.js` 与 `dist/.openai/hosting.json` 不变。
- 已发布 Sites 版本 3；公开地址继续使用 <https://guizhou-six-day-guide.rvsinw.chatgpt.site>。
- 已核查用户提供的手机浏览器截图，确认页面为 Cloudflare “Attention Required” 安全拦截，而不是网站自身 404、白屏或 React 运行错误。
- 已使用 iPhone Safari、Android Chrome 和微信 Android User-Agent 请求现有地址，均得到 HTTP 200；同时确认 Sites 项目处于公开、启用、版本 3 生效状态。
- 已向用户说明三个处理方向；用户选择 GitHub Pages + Actions 自动构建方案，并明确批准创建公开仓库 `guizhou-six-day-guide`，允许使用已登录的 GitHub Desktop/网页端完成发布。
- 已形成 GitHub Pages 手机端公开访问设计说明：`docs/superpowers/specs/2026-07-16-github-pages-mobile-access-design.md`。
- 用户已确认 GitHub Pages 手机端公开访问设计，允许进入实施计划与发布阶段。
- 已形成 GitHub Pages 实施计划：`docs/superpowers/plans/2026-07-16-github-pages-mobile-access.md`。
- 用户选择在当前任务中直接执行实施计划；已用测试先行方式新增 GitHub Pages 工作流契约测试，确认其先因工作流不存在而失败，再加入最小权限工作流使测试通过。
- 本地完整测试已增至 16 项并全部通过；`npm run build` 成功，10 个关键入口、Sites 文件和本地图片产物全部存在。Pages 实现提交为 `02cefa8`。
- GitHub Desktop 已添加当前本地仓库并识别 `main` 分支、0 个未提交文件；当前停在创建公开仓库 `guizhou-six-day-guide` 的最终 “Publish repository” 动作前，等待公开权限变更的动作时确认。
- 用户已在动作时确认公开发布；GitHub 远端已创建为 <https://github.com/LARRY-LLM/guizhou-6day-guide>，`main` 已推送并指向提交 `93b3e84`。
- Windows 自动操作第二次仍无法可靠确认 Chrome 当前 URL；用户改为人工完成 GitHub Pages 开关。已提供 Pages 设置页和 Actions 工作流页的直接地址，以及选择 “GitHub Actions” 和重新运行工作流的具体点击步骤。
- 用户截图确认 GitHub 已识别 `Deploy GitHub Pages` 工作流和 `workflow_dispatch` 手动触发器；提交 `93b3e84` 的首次运行在 9 秒后失败，页面已提供 “Run workflow” 入口。下一步是确认 Settings → Pages 的 Source 为 “GitHub Actions”，再从 `main` 手动重跑。
- 用户手动重跑的 `Deploy GitHub Pages #2` 在 25 秒后失败。通过 GitHub Actions Jobs API 复核，检出、Node 配置、Pages 配置、依赖安装、生产构建和 Pages 产物上传全部成功，唯一失败步骤是 `Post Set up Node.js`。
- 根因是仓库 `.npmrc` 写死 Windows 本机绝对缓存路径，Ubuntu Actions 运行器在 Node 缓存收尾时无法找到该路径。新增回归测试先复现失败，再删除项目级 `cache=` 覆盖；目标构建测试 7/7、完整测试 17/17 和生产构建均通过。
- 修复提交 `85363c3` 已推送；GitHub Actions 运行 `29482831847` 完成并返回 `success`，GitHub Pages 正式地址为 <https://larry-llm.github.io/guizhou-6day-guide/>。

### 验证结果

- Vitest：17 项测试全部通过。
- 生产构建：`npm run build` 通过，生成 `dist/client/`、`dist/server/index.js` 与 `dist/.openai/hosting.json`。
- 本地浏览器：页面标题、根节点与主标题正常，控制台无 warning/error，页面无横向溢出。
- 旧版本复现：版本 2 的 `/`、`/index.html` 和实际 JS 均返回 HTTP 404，证明问题不在访问权限或单一路由。
- 新版本公网验证：`/`、`/index.html`、HTML 引用的实际 JS/CSS 连续三轮全部返回 HTTP 200，且页面根节点存在。
- 新版本真实浏览器：主标题与完整正文成功渲染，15/15 张图片加载，页面无横向溢出，控制台无 warning/error。
- GitHub Pages 公网验证：根页面、实际 JS 和实际 CSS 连续三轮全部返回 HTTP 200。
- GitHub Pages 独立浏览器：页面标题与主标题正确，React 完整正文已渲染；滚动触发懒加载后 15/15 张图片完整，无横向溢出，控制台无 warning/error。

### 遇到问题与处理

1. 首次发布时，带构建归档上传连续两次在托管文件传输端失败；归档文件本身完整，约 19 MB，改用纯英文临时路径后仍失败。
2. 改用已推送的源码构建后，平台最初因锁文件中使用 `registry.npmmirror.com` 而拒绝依赖。替换为官方 npm 注册表后，部署状态变为成功。
3. 当前部署未能对外提供任何静态入口，表现为根路径和已知文件路径均为 404。对照 Sites 的可运行产物后确认，Worker 入口与托管配置存在，但浏览器文件位于 `dist/` 根目录，不在平台用于 `ASSETS` 绑定的 `dist/client/`；因此部署状态成功而所有静态请求仍为 404。
4. 版本 3 的约 20 MB 构建归档再次在托管文件传输端失败；改用平台从同一已推送提交执行干净构建，发布成功，未改变已验证的源码。
5. 用户在手机浏览器通过移动网络访问 Sites 地址时被 Cloudflare 安全服务拦截。由于同一版本在桌面真实浏览器完整渲染，多个移动 User-Agent 请求也返回 200，说明拦截发生在应用代码之前，并依赖特定手机网络出口或边缘风控信号；`chatgpt.site` 的托管安全规则不由该项目控制，无法从 React/Vite 源码中关闭。解决方向是改用 GitHub Pages 提供独立的公开访问链路，同时保留 Sites 作为备用。
6. GitHub Desktop 在第一次填写本地仓库路径后读取界面状态时返回一次 UI 可访问性缓存错误；停止使用旧控件索引、重新定位 GitHub Desktop 窗口后确认表单仍在且未提交，重新填写路径并成功添加仓库，未发生错误上传或重复仓库创建。
7. GitHub Desktop 首次创建远端后界面仍显示上传动画，远端起初没有分支；使用 “Publish branch” 重试后，通过 `git ls-remote` 确认 `main` 已到达远端。随后切换 Chrome 时，Windows 自动操作因无法可靠确认浏览器当前 URL 而按安全规则停止；用户已将 GitHub 标签页置于前台并要求继续。
8. Pages 第二次运行的页面注释显示缓存路径校验错误。Jobs API 进一步证明实际站点构建和产物上传成功，失败只发生在 `actions/setup-node` 的缓存收尾；向上追溯到 `.npmrc` 的 Windows 绝对缓存目录，而不是 React/Vite、Pages 权限或产物目录。通过移除平台相关缓存覆盖并增加自动化契约测试修复。
9. 修复提交首次通过命令行推送时卡在本机凭据代理连接，GitHub Desktop 随后报告 `The remote disconnected`。终止由该次超时命令启动的残留 Git 进程后，使用 GitHub Desktop 做一次干净重试，3 个增量提交成功到达 `origin/main`，新 Actions 运行自动开始。

### 沟通与记录约定

- 用户要求所有沟通过的任务和问题都写进本文件。今后每项用户任务、问题、决定、诊断证据、处理结果和阻塞均应及时补充，不再只在阶段结束时汇总。

### 下一步

1. 用户在此前被 Cloudflare 拦截的手机浏览器和网络下打开 GitHub Pages 新地址并验收。
2. 完成打印另存 PDF 人工核查；全部验收后结束阶段 4。

## 阶段 5：完整攻略内容替换与六日地图（2026-07-17）

### 用户决定

- 在现有 React/Vite 网页基础上修改，不另起一套网站。
- 网站全部内容以 `D:/workbuddy/project/2026-07-16-16-34-33/贵州6天攻略_完整整合版.md` 为准，并继续使用此前的“黔蓝手作旅行簿”风格。
- 采用已确认的模板 2，加入 6 天行程地图，并兼顾视觉美化。
- 启用子代理并行完成数据、样式和地图素材，再由内容、集成、视觉三路独立复核。

### 本阶段完成内容

- 新建 `src/data/guide.js` 与 `src/data/days.js`，录入 D1–D6 日程、逐段交通、全部价格、12 家酒店、餐饮、预算、预约、避坑、来源与时效说明。
- 新建 `RouteMap`、`TripOverview`、`DayChapter`、`TripSummaries` 组件，形成“路线地图 → 行前总览 → 六日逐日手记 → 交通住宿汇总 → 吃喝避坑”的完整长页。
- 生成 2048 × 1152 的贵州六日环线地图位图 `public/assets/guizhou-route-map.png`；路线为贵阳 → 黄果树/安顺 → 龙宫/屯堡 → 贵阳 → 西江 → 朗德 → 贵阳返程。
- D3 主路线明确从安顺住宿点出发，黄果树直转龙宫仅保留为备选；D5 固定返回西江续住，住朗德明确标为非主路线备选。
- 补入“每天车程 ≤ 2.5h、不赶路”、7–8 月 18–28℃、资料来源构成与出发前二次核验说明。
- 移除旧 `src/data/itinerary.js` 和 `public/assets/xiaoqikong-bridge.png`；源码与生产产物不再包含荔波、小七孔、文昌阁、电台街旧路线。
- 更新浏览器标题、描述、导航、打印入口、A4 样式和移动端 `data-label` 表格。

### 问题与修复

1. 内容首轮复核发现行程节奏、气温和来源构成遗漏；补入数据层并在首屏、行前总览和来源区实际渲染。
2. D3 页头最初从黄果树起，与 D2 已住安顺冲突；改为安顺主线，并把黄果树直转写成备选起点。
3. 旧小七孔图片仍会被 Vite 复制到生产产物；删除旧数据和图片后重新构建，production legacy scan clean。
4. 行前总览标题容器漏了统一页面边距；补充桌面、手机和打印宽度约束后，标题与台账左右对齐。
5. 桌面地图 D2/D3 标签因统一向右展开而相交；让 D2 向左展开、标签宽度控制在 145–170px，同视口 DOM 几何检查为 `overlaps=[]`。
6. 初版测试只保护主要字段；扩展为与数据同构的完整渲染断言，覆盖 location/stay、耗时/提示、section 标题、hotelNote、行前清单、预算、汇总表、foodTips 与全部来源元数据。

### 验收结果

- 内容复核：PASS；原稿全部模块已进入页面，没有剩余路线冲突或渲染遗漏。
- 集成复核：PASS；旧路线物料已移除，组件数据消费、ARIA 表格语义、导航与打印入口正常。
- 视觉代码复核：PASS；仅保留可接受 P3——手机端节点清单与六日速览略有信息重复。
- Vitest：26/26 通过。
- 生产构建：`npm run build` 成功，产物输出到 `dist/client/`。
- 浏览器视口：1440 × 900、1024 × 900、390 × 844 均无横向溢出。
- 响应式：行前总览 4/2/1 列；手机路线节点为单列；长表格显示字段标签。
- 图片：路线地图及所有页面图片均返回有效自然尺寸；没有用 CSS/手写 SVG 伪造核心视觉素材。
- 交互：顶部路线/行前锚点和地图 D3 节点定位成功；打印按钮自动化交互测试通过。
- 控制台：warning/error 为空。
- 设计 QA：`design-qa.md` 最终结论为 `final result: passed`；关键对照图为 `design-qa/comparison-route-map-final-pass.png`。

### 下一步

1. 用户打开本地预览确认内容密度和视觉细节。
2. 用户确认后，可把当前版本推送到 GitHub Pages，替换现有公开网站。
